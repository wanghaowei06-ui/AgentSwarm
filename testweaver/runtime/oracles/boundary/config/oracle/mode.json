{
  "schema_version": "testweaver.oracle-runtime-mode/v1",
  "oracle_kind": "boundary",
  "python_module": "verifier.py",
  "entrypoint": "verify_boundary",
  "allowed_inputs": ["observation", "manifest", "public_boundary"],
  "forbidden_inputs": ["gold", "outcome_oracle_artifacts"],
  "emits": "boundary_result_ref_only"
}
